# Archon UI Next.js - Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================================================
# Backend API Configuration
# ============================================================================

# Main Archon API (port 8181)
NEXT_PUBLIC_API_URL=http://localhost:8181

# ============================================================================
# Optional Services
# ============================================================================

# Agent Work Orders Microservice (port 8053)
# This service is optional. If not running, the UI will gracefully handle its absence.
# Uncomment to enable agent work orders features:
# NEXT_PUBLIC_AGENT_WORK_ORDERS_URL=http://localhost:8053

# Disable agent work orders completely (skip connection attempts)
# Set to true if the service is not running to prevent console errors:
NEXT_PUBLIC_DISABLE_AGENT_WORK_ORDERS=true

# ============================================================================
# Feature Flags
# ============================================================================

# These are handled by /api/credentials/[key] endpoint
# No environment variables needed - configured in code

# ============================================================================
# Development Options
# ============================================================================

# Next.js environment
NODE_ENV=development

# ============================================================================
# Notes
# ============================================================================

# 1. The main API (port 8181) is required for all features
# 2. Agent Work Orders (port 8053) is optional - UI works without it
# 3. Missing services will fail gracefully without console errors
# 4. For production, update URLs to use your deployment domains
